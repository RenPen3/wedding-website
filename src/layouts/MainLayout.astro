---
import '../styles/global.css';
import ScrollReveal from '../components/ScrollReveal.astro';
import InvitationIntro from '../components/InvitationIntro.astro';
import InviteOpenTracker from '../components/InviteOpenTracker.astro';
import PostHog from '../components/posthog.astro';

type Props = {
	title?: string;
	description?: string;
	showInvitationIntro?: boolean;
	noindex?: boolean;
};

const {
	title = 'Jocelyn & René — September Wedding',
	description = 'Join us for our wedding celebration.',
	showInvitationIntro = false,
	noindex = false,
} = Astro.props;
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<script is:inline>
			try {
				if (localStorage.getItem('wedding-lang') === 'es') {
					document.documentElement.classList.add('lang-es');
					document.documentElement.setAttribute('lang', 'es');
				}
			} catch (_) {}
		</script>
		<script is:inline>
			// Prevent intro flash if already opened this session.
			try {
				if (sessionStorage.getItem('wedding-invitation-opened') === '1') {
					document.documentElement.classList.add('invitation-intro-seen');
				}
			} catch (_) {}
		</script>
		<meta name="viewport" content="width=device-width, initial-scale=1" />
		<link rel="icon" href="/favicon.ico" sizes="32x32" />
		<link rel="icon" type="image/png" sizes="192x192" href="/favicon.png" />
		<link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
		<meta name="generator" content={Astro.generator} />
		<meta name="description" content={description} />
		{noindex && <meta name="robots" content="noindex, nofollow" />}
		<title>{title}</title>
		<PostHog />
		<link rel="preconnect" href="https://fonts.googleapis.com" />
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
		<link
			href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,400;0,600;0,700;1,400&family=Outfit:wght@300;400;500;600&display=swap"
			rel="stylesheet"
		/>
	</head>
	<body class="min-h-screen">
		<InviteOpenTracker />
		{showInvitationIntro && <InvitationIntro />}
		<slot />
		<ScrollReveal />
	</body>
</html>
